{% if products.count > 0 %}
<div class="receipt-sizes">
     <h4>
         Выбери размер:
     </h4>
    <div class="receipt-sizes-container">
    {% for product in products %}
        <div class="size-block" data-name="{{ product.id }}" data-size-price="{{ product.price }}">
            {{ product.ProductSize }}
        </div>
    {% endfor %}
    </div>
</div>
{% else %}
    <div class="parent">
        <div class="no-allowed-sizes" data-name="{{ id }}" data-type="receipt">
                <div class="nas-title">
                        Извини, доступных размеров на сегодня нет. Заказать на другой день?
                </div>
                <div id="another-type" class="custom-button warn-button" style="margin: 0">
                    Заказ на другой день
                </div>
        </div>
    </div>
{% endif %}
